services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: gathix
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - gathix_data:/var/lib/postgresql/data

volumes:
  gathix_data: